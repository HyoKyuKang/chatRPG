# V1.0 Blocker — 출시 전 fix 필수

V2 HOUR 5 / 5c 친구 베타 후 채움. 재현 가능한 크래시 / 진행 막히는 버그만.

기준:
- ✅ 재현 가능 (다른 친구도 같은 곳에서 막힘)
- ✅ 출시 차단급 (앱 자체가 안 됨)
- ✅ V1.0 출시 전 fix 가능 (스코프 안)

기준 미달 catch (UX / 글 톤 / 새 기능 요청) → `v1.1-backlog.md` 로.

---

## 발견된 blocker

(빈 placeholder. 베타 후 채움.)

---

## 이미 알려진 위험 (5b 빌드 시점에 monitor)

- [ ] Lightsail 빌드 → APK 가 실제 폰 (Android 7+) 에서 정상 실행되는가
- [ ] Korean 앱 이름 (한글 strings.xml) 모든 launcher 에서 정상 표시
- [ ] localStorage persist 가 Capacitor WebView 에서 작동
- [ ] 회색의 자 등장 시 portrait image 로드 (offline 시 dist 의 file 가 sync 됐나)
- [ ] 메타 잠금해제 화면 의 ⟲ DEV reset 버튼 = production build 에선 사라짐 (`import.meta.env.DEV` 가드)
